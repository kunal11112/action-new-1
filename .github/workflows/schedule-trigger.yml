name: schedule trigger event

on:
 schedule:
   - cron: "*/1 * * * *"

jobs:
  schedule job:
     runs-on: ubuntu-latest
     steps:
     - name: this is scheudle job
       run: echo "this is scheudle job"